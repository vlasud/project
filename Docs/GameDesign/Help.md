# Справка игрока — `/help`

Дизайн-спецификация команды `/help` и единый источник текстов описаний всех
команд для разработчика. Технику диспетчера и реестра команд см.
`Docs/CommandFlood.md` и код `PlayerCommandService`; общий свод UI-тона —
`Docs/GameDesign/UI_Texts.md` (этот док — его продолжение по теме справки).

`/help` — это младший брат `/ahelp` (см. UI_Texts.md, раздел «/ahelp»): тот же
вид диалога, тот же стиль описаний, но другой охват. `/ahelp` — памятка админа
(служебный тулинг по уровню), `/help` — памятка обычного игрока (геймплей).

---

## 1. Что такое `/help` для игрока

Персональная памятка «что я сейчас могу набрать в чат». Это НЕ энциклопедия всех
команд сервера: игрок видит только то, что доступно лично ему прямо сейчас.
Открыл — прочитал — закрыл. Реакции на выбор строки нет (как у `/ahelp`).

Зачем нужна: у гейммода десятки команд, разбросанных по системам (чат/RP,
деньги, банк, выборы, владение оружием, фракция). Новичок не знает, что вообще
существует; член фракции не помнит, какие у него команды появились со вступлением.
`/help` — единственная точка, где это собрано человеческим языком, без чтения вики.

---

## 2. Охват: какие команды показывать

### Решение

`/help` показывает игроку **все геймплейные команды (Kind::None)** плюс
**фракционные команды, если игрок состоит в организации**. Админ- и dev-тулинг
(Kind::AdminLevel) в `/help` НЕ входит — для него отдельный `/ahelp`.

### Важная оговорка по реальной механике прав (отличие от рамки ТЗ)

В реестре команд фракционные команды (`/faction`, `/invite`, `/uninvite`,
`/setrank`, `/setsalary`, `/r`, `/skin`, `/gov`) зарегистрированы НЕ как
`FactionMember`/`FactionPermission`, а как **`Kind::None`** — они открыты
диспетчером всем, а право проверяют **внутри обработчика** (лидерство,
`PERM_INVITE`, `PERM_SKIN`, кураторство). То есть резолвер прав про их
«фракционность» ничего не знает.

Из этого следует:

- **Нельзя** строить охват `/help` тем же приёмом, что `/ahelp` (он фильтрует
  `collectAccessible` по `Kind::AdminLevel`). Для `/help` фильтр по `Kind` даст
  ВСЕ `None`-команды одинаково — и геймплейные, и фракционные, — и не отличит
  «я в фракции» от «я гражданин».
- Поэтому **категория команды для `/help` задаётся декларативно в этом доке и в
  данных команды**, а не выводится из `PermissionSpec`. Разработчик отмечает у
  каждой команды её категорию (см. таблицу в разделе 5). `/help` показывает:
  все команды категорий «Чат и RP», «Экономика», «Прочее» — всегда; команды
  категории «Фракция» — только если `getMemberFaction(playerId) != NO_FACTION`.

Это честнее для игрока, чем показывать фракционные команды всем: гражданин,
который не в организации, не должен видеть в своей справке `/r` или `/setsalary` —
он их не может применить, и они для него шум.

### Подтверждение рамки и обоснование

Рамка «геймплей + фракция, без админки» правильная — она ровно повторяет
ментальную модель игрока: «обычные команды» и «команды моей работы». Развилка с
ТЗ только в способе определить фракционность (по факту членства, а не по
`PermissionSpec`), потому что так устроен код.

Граничные случаи охвата:

- **`/edev` и `/wskill`** — это dev-меню, ошибочно зарегистрированные как
  `Kind::None` (открыты всем). В `/help` их быть НЕ должно — категория «Админ/dev»
  в таблице, не показываются игроку. (Замечание разработчику: по уму им нужен
  `PermissionSpec::admin(DEVELOPER_LEVEL)`, как у `/fdev` и прочего dev-тулинга;
  пока этого нет, `/help` исключает их по категории, а не по правам.)
- **`/mute` и `/unmute`** — временная модерация, тоже открыта всем (`Kind::None`),
  но это де-факто админ-инструмент. В `/help` НЕ показываем (категория «Админ/dev»).
  Замечание то же: им нужен админ-порог.

---

## 3. Вид диалога

`DialogStyle_TABLIST_HEADERS`, две колонки «Команда | Описание» — один в один с
`/ahelp`, чтобы две справки выглядели как одна семья. Игрок, открыв `/help` и
`/ahelp`, видит одинаковую вёрстку — меньше когнитивной нагрузки.

| Элемент | Текст / решение |
|----|----|
| Стиль | `DialogStyle_TABLIST_HEADERS` |
| Заголовок диалога | `Команды` |
| Шапка колонок | `Команда` \| `Описание` |
| Разделитель группы | `{FFB400}—— {Категория} ——` (в колонке «Команда»; «Описание» пустая) |
| Строка команды | `/{имя}` \| `{описание}` |
| Кнопка слева | `Закрыть` |
| Кнопка справа | пусто (только просмотр) |

### Группировка и сортировка

**Группировать по категориям** (а не валить плоским списком). Категории и порядок
секций фиксированы — это «оглавление» справки, оно не должно прыгать:

1. **Чат и RP** — то, чем игрок пользуется чаще всего;
2. **Экономика** — деньги и банк;
3. **Фракция** — секция появляется ТОЛЬКО у члена организации;
4. **Прочее** — выборы, владение оружием, сам `/help`.

Внутри каждой секции — **сортировка по имени команды** (как внутри уровня в
`/ahelp`). Так список детерминирован и не зависит от порядка регистрации систем.

Обоснование группировки: у обычного игрока команд немного (≈10–15 даже с
фракцией), плоский список тоже читался бы, но категории с жёлтым разделителем
дают игроку «карту»: видно, что есть отдельный блок про деньги и отдельный про
фракцию. Это та же логика, что разделители уровней в `/ahelp`, — и тем же цветом
`{FFB400}`, чтобы заголовки секций «всплывали» над белыми строками команд.

### Разделитель

Тот же символ, что в `/ahelp`: длинное тире `——` (U+2500 недоступен в cp1251,
используется фолбэк `——`, как уже сделано в коде `/ahelp`). НЕ тройной дефис.
Формат: `—— {Категория} ——`, уровень/номер не пишем (у игрока категории, а не
числовые уровни — это в духе столпа «без числовых лестниц», GDD).

### Пустых секций не бывает

Секция «Фракция» либо есть целиком (игрок в организации), либо отсутствует. Внутри
«Чат и RP», «Экономика», «Прочее» всегда есть команды — пустого «Нет команд» в
`/help` не возникает (в отличие от граничного `/ahelp` для не-админа). Если по
какой-то причине список пуст — строка `Нет доступных команд` в колонке «Команда»
(тот же фолбэк, что у `/ahelp`).

---

## 4. Тон и стиль описаний

Единый с `/ahelp` (UI_Texts.md): **безличный глагол в инфинитиве** для
команд-действий («передать», «написать», «открыть») и назывная группа для
команд-меню («меню фракции», «навыки владения оружием»). Со строчной буквы (идёт
второй колонкой после `/имя`), без точки в конце, коротко (влезает в колонку
tablist, ~5–9 слов). Жаргон реализации не используется — пишем, что игрок
получает, а не как это устроено.

Отличие от `/ahelp` по содержанию: описания игрока не должны вскрывать радиусы,
кулдауны и пороги (это столп «не подсказывать обход», UI_Texts.md). Поэтому
«передать наличные игроку рядом», а не «…в радиусе 5 м»; «написать всем в
организации», а не «всем членам онлайн в радиусе мира».

---

## 5. Полная таблица: команда → описание → категория → охват

Категория задаёт секцию `/help` и порядок. Охват: **player** — видна всем в
`/help`; **faction** — видна в `/help` только члену организации; **admin** — в
`/help` НЕ показывается (живёт в `/ahelp`, описания дублируют UI_Texts.md как
единый источник после переноса описаний в реестр).

Описания админ/dev-команд приведены к финальным формулировкам из UI_Texts.md
(раздел «/ahelp»), чтобы при переносе описаний в реестр разработчик взял текст
отсюда без расхождений.

### Категория «Чат и RP» (охват: player)

| Команда | Описание | Источник (система) |
|----|----|----|
| `/me` | описать своё действие в ролевом чате | RoleplayChatSystem |
| `/do` | описать обстановку или состояние вокруг | RoleplayChatSystem |
| `/try` | описать действие с исходом удача/провал | RoleplayChatSystem |

### Категория «Экономика» (охват: player)

| Команда | Описание | Источник (система) |
|----|----|----|
| `/pay` | передать наличные игроку рядом | PaymentSystem |
| `/balance` | показать баланс банковского счёта | BankSystem |

### Категория «Фракция» (охват: faction — только член организации)

| Команда | Описание | Источник (система) |
|----|----|----|
| `/faction` | меню организации: состав, ранги, бюджет | FactionSystem |
| `/r` | рация: написать всем в своей организации | FactionSystem |
| `/skin` | сменить скин из пула организации | FactionSystem |
| `/invite` | пригласить игрока в организацию | FactionSystem |
| `/uninvite` | уволить игрока из организации | FactionSystem |
| `/setrank` | назначить игроку ранг в организации | FactionSystem |
| `/setsalary` | назначить игроку зарплату | FactionSystem |
| `/gov` | управление лидерами подопечных фракций | FactionSystem |

Пояснение: `/invite`, `/uninvite`, `/setrank`, `/setsalary`, `/gov` доступны не
каждому члену, а только лидеру/обладателю права/куратору — но проверяется это
внутри команды. В `/help` их показываем всем членам организации (см. раздел 6,
риск «лишние строки»): попытка применить без права даст внятный отказ команды.

### Категория «Прочее» (охват: player)

| Команда | Описание | Источник (система) |
|----|----|----|
| `/mn` | меню игрока: информация, связь с администрацией, помощь | PlayerMenu |
| `/car` | меню личного транспорта: показать машину на карте | CarMenuSystem |
| `/gps` | проложить маршрут к нужному месту | GpsSystem |
| `/skills` | навыки владения оружием | WeaponProficiencySystem |
| `/help` | список доступных вам команд | (новая) |

### Категория «Админ/dev» (охват: admin — в `/help` НЕ показывается)

Описания — финальные из UI_Texts.md, приведены здесь как единый источник для
переноса в реестр команд. `/edev`, `/wskill`, `/mute`, `/unmute` — сейчас
ошибочно `Kind::None`, но по смыслу dev/модерация: в `/help` не входят.

| Команда | Описание | Порог (как в коде) |
|----|----|----|
| `/mn` | меню игрока: информация, связь с администрацией, помощь | None |
| `/help` | (см. «Прочее») | None |
| `/ahelp` | список доступных вам админ-команд | admin(1) |
| `/a` | админ-чат: написать всем админам онлайн | admin(1) |
| `/kick` | кикнуть игрока с сервера с указанием причины | admin(1) |
| `/an` | отправить игроку личное сообщение от администрации | admin(1) |
| `/goto` | телепортироваться к игроку по id | admin(1) |
| `/gethere` | телепортировать игрока к себе по id | admin(1) |
| `/ban` | забанить аккаунт на срок в днях с указанием причины | admin(3) |
| `/setadmin` | выдать или снять уровень админки (0–5) | admin(5) |
| `/alogin` | войти в админку по паролю | None (проверка внутри) |
| `/mute` | замутить игрока в чате на N секунд | None (по смыслу — модерация) |
| `/unmute` | снять мут с игрока | None (по смыслу — модерация) |
| `/session` | показать данные своей сессии: аккаунт, время в игре | None (dev-проба, не для игрока) |
| `/veh` | заспавнить машину по модели и сесть в неё | admin(dev) |
| `/vput` | сесть в ближайшую машину (до 50 м) | admin(dev) |
| `/vdel` | удалить машину, в которой сидишь | admin(dev) |
| `/vrespawn` | переспавнить машину (HP → 1000) | admin(dev) |
| `/vinfo` | информация о машине: HP сервер/клиент, посадочное место | admin(dev) |
| `/vhp` | задать HP машины (0–1000) | admin(dev) |
| `/vrepair` | отремонтировать машину | admin(dev) |
| `/vengine` | завести или заглушить двигатель | admin(dev) |
| `/vlock` | запереть или отпереть двери машины | admin(dev) |
| `/vtune` | меню тюнинга своей машины: компоненты, цвет, пейнтджоб | admin(dev) |
| `/vhack` | тест анти-чита: имитировать чит ремонта машины | admin(dev) |
| `/pos` | показать свои координаты, мир и интерьер | admin(dev) |
| `/tp` | телепорт к выбранному месту каталога (меню) | admin(1) |
| `/tpxyz` | телепортироваться в координаты X Y Z | admin(dev) |
| `/tpup` | подбросить себя вверх на N метров (тест падения) | admin(dev) |
| `/vw` | сменить виртуальный мир | admin(dev) |
| `/int` | сменить интерьер | admin(dev) |
| `/hackpos` | тест анти-чита: имитировать чит телепорта | admin(dev) |
| `/vel` | показать текущую скорость | admin(dev) |
| `/veldebug` | включить или выключить уведомления о скорости | admin(dev) |
| `/violations` | показать свой журнал нарушений анти-чита | admin(dev) |
| `/acclear` | очистить свой журнал нарушений | admin(dev) |
| `/editor` | редактор объектов (меню) | admin(dev) |
| `/td` | редактор текстдро (меню) | admin(dev) |
| `/gzone` | редактор ганг-зон (меню) | admin(dev) |
| `/gzhere` | показать, в какой ганг-зоне вы стоите | admin(dev) |
| `/camera` | дев-камера: облёт по точкам (меню) | admin(dev) |
| `/cpoint` | сохранить точку маршрута дев-камеры | admin(dev) |
| `/cplay` | проиграть облёт по сохранённым точкам | admin(dev) |
| `/gridtest` | построить тестовое поле объектов вокруг себя | admin(dev) |
| `/gridclear` | убрать тестовое поле объектов | admin(dev) |
| `/gridcell` | включить или выключить уведомления о смене ячейки | admin(dev) |
| `/streamdebug` | включить или выключить уведомления стримера | admin(dev) |
| `/near` | показать сущности в заданном радиусе | admin(dev) |
| `/rof` | замерить темп стрельбы (тест анти-rapid-fire) | admin(dev) |
| `/spec` | наблюдать за игроком по id | admin(dev) |
| `/specoff` | выйти из режима наблюдения | admin(dev) |
| `/edev` | дев-меню выборов (меню) | None (по смыслу — dev) |
| `/wskill` | дев-меню навыков оружия (меню) | None (по смыслу — dev) |
| `/fdev` | дев-меню фракций (меню) | admin(dev) |
| `/sethp` | задать игроку здоровье (0–100) | admin(4) |
| `/setarmour` | задать игроку броню (0–100) | admin(4) |
| `/slap` | подбросить игрока вверх | admin(2) |
| `/agun` | выдать игроку оружие с патронами | admin(5) |
| `/askin` | выдать игроку временный скин | admin(4) |
| `/devskin` | сменить игроку основной скин | admin(6) |
| `/savepos` | запомнить свои координаты | admin(1) |
| `/tppos` | телепортироваться к запомненным координатам | admin(1) |
| `/gm` | включить или выключить бессмертие | admin(1) |

Примечание: `GridDebugSystem` (`/gridtest /gridclear /gridcell /streamdebug /near`)
сейчас отключён в `SystemRegister.cpp` — описания в реестре проставлены, но в
рантайме команды не регистрируются, пока система не подключена. На `/help`/`/ahelp`
это не влияет (незарегистрированные команды нигде не показываются).

---

## 6. Риски UX и дизайн-оценка

- **Лишние строки у рядового члена фракции.** Рядовой член без права найма видит
  `/invite`, `/setrank` и т.п., но применить не может. Решено сознательно
  показывать их всем членам: (1) граница «кому что можно» в фракциях
  динамическая (ранг могут поднять прямо в сессии), и фильтровать справку по
  текущему праву — это лишняя сложность ради малого выигрыша; (2) видеть полный
  набор команд своей организации полезно (игрок понимает, к чему стремиться по
  рангу); (3) попытка применить без права даёт внятный отказ самой команды.
  Если позже захочется чище — можно прятать командирские строки за
  `PERM_INVITE`/лидерством, но это **nice-to-have**, не блокер.
- **Дрейф справки от реального набора команд.** `/help` — статический список из
  этого дока, а команды живут в коде. Риск: добавили команду, забыли в `/help`.
  Снимается тем, что описание переезжает в реестр (аргумент `description` у
  `add()`), а `/help`/`/ahelp` строят список из реестра — тогда новая команда
  автоматически и описана, и в нужной справке. Это архитектурное решение
  разработчика; со стороны дизайна — поддерживаю (единый источник истины).
- **`/mute`, `/unmute`, `/edev`, `/wskill` открыты всем.** Это не риск самого
  `/help` (мы их прячем по категории), но дизайн-замечание: модерация и dev-меню
  не должны быть доступны рядовому игроку. Нужен админ-порог — отдельной задачей.
- **Длина списка.** У игрока в крупной фракции ≈18 строк с заголовками секций —
  tablist прокручивается, читается нормально. Перегруза нет.

**Вердикт:** `/help` вписывается в проект — это прямой парный близнец `/ahelp`,
тот же вид и тон, закрывает реальную дыру (новичок не знает команд). Главное
решение — охват по факту членства во фракции + категории из дока, а не по
`PermissionSpec` (так устроен код).
