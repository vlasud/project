#include "Services/PlaceCatalogService/PlaceCatalogService.h"

PlaceCatalogService::PlaceCatalogService()
{
    // Координаты — реального мирового контента (пикапы работ, входы баз, стойки).
    // Порядок = порядок строк во всех списках мест; добавляя точку, помни, что она
    // сдвигает индексы ниже (сами индексы нигде не сохраняются, только в живом
    // диалоге, поэтому это безопасно).
    m_places = {
        {"Порт — работа грузчиком", {2746.6726f, -2450.3950f, 13.6484f}},
        {"Автобусное депо — работа водителем", {1204.0439f, -1823.2920f, 13.5918f}},
        {"Депо развозчиков — работа развозчиком", {2171.0681f, -2252.5254f, 13.3026f}},
        {"Больница — работа врачом", {1183.6260f, -1332.1185f, 13.5814f}},
        {"Таксопарк — работа таксистом", {1076.2310f, -1779.8088f, 13.5837f}},
        {"Центральная парковка", {1626.2488f, -1136.7443f, 23.9063f}},
        {"Банк (Сан-Фиерро)", {-2766.5515f, 375.5889f, 6.3347f}},
        {"Мэрия (Лос-Сантос)", {1480.94f, -1772.07f, 18.80f}},
    };
}

const std::vector<PlaceCatalogService::Place> &PlaceCatalogService::places() const
{
    return m_places;
}
